import { GetRequestResponse } from "../requests/types";

export const handleTVAnime = async (request: GetRequestResponse) => {
  return true;
};

export const handleTVNonAnime = async (request: GetRequestResponse) => {
    //Check how many seasons are in the request and how many there are in total
  return true;
};